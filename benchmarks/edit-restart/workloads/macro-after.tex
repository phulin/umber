\input plain.tex
\hsize=220pt \vsize=240pt
\def\variant{omega}
The first paragraph consumes \variant\ among ordinary words and repeats the value \variant\ before ending.

The second paragraph also consumes \variant\ among otherwise unchanged words used to validate meaning dependencies.

The third paragraph has no dependency on the changed macro and remains an unchanged restart suffix.
\bye
