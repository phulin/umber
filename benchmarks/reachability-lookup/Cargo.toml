[package]
name = "reachability-lookup-benchmark"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
criterion = "0.5"
tex-state = { path = "../../crates/tex-state", features = ["testing"] }

[[bench]]
name = "lookup"
harness = false
