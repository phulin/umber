[package]
name = "format-restore-benchmark"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
criterion = "0.5"
tex-state = { path = "../../crates/tex-state", features = ["profiling"] }

[[bench]]
name = "decode"
harness = false

[lints.clippy]
undocumented_unsafe_blocks = "deny"
