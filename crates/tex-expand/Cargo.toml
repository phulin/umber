[package]
name = "tex-expand"
version.workspace = true
autotests = false
edition.workspace = true

[features]
shadow = []
profiling = []

[dependencies]
tex-command = { path = "../tex-command" }

[lints]
workspace = true
