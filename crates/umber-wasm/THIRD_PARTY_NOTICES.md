# Third-party notices

The packaged `assets/plain.fmt` is generated by Umber from the exact TeX Live
2025 inputs listed in `assets/plain-source.lock`.

- Donald E. Knuth's `plain.tex` and Computer Modern TFM data are distributed by
  TeX Live under the Knuth license. Modified files must not use the original
  filenames.
- Knuth's `hyphen.tex` patterns permit unlimited copying and redistribution
  when unchanged. Modified versions are permitted only under a different
  filename.

The generated format is Umber-native and is not a TeX Live engine format. The
source files themselves are not included in this package. This notice does not
grant a license to Umber; the npm metadata reports `UNLICENSED` until the
repository owner selects a project license.
