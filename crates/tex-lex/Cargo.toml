[package]
name = "tex-lex"
version.workspace = true
edition.workspace = true

[features]
expansion-stats = []

[dependencies]
tex-state = { path = "../tex-state" }

[dev-dependencies]
tempfile.workspace = true

[lints]
workspace = true
