[package]
name = "tex-lex"
version.workspace = true
edition.workspace = true

[features]
# Compile-time counters for dedicated profiling builds.
profiling = ["tex-state/profiling"]

[dependencies]
ahash.workspace = true
tex-state = { path = "../tex-state" }

[dev-dependencies]
tempfile.workspace = true
tex-state = { path = "../tex-state", features = ["testing"] }

[lints]
workspace = true
