# Runtime storage external contract ledger

Status: pre-rewrite semantic baseline for
[`runtime_storage_lifetimes.md`](runtime_storage_lifetimes.md).

This ledger freezes behavior that the runtime-storage rewrite must preserve.
The owners below assert TeX-visible channels, detached artifacts, host
protocols, or cold-run equivalence. Allocation order, pointer identity,
generation numbers, arena or region keys, owner counts, weak-index shape,
journal representation, and Rust type size are deliberately not compatibility
contracts.

## Retained contracts

| Contract                      | Authoritative retained owner                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Evidence and tier                                                                                                                                                                                                                                                                                                                   |
| ----------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TRIP                          | `crates/umber/tests/it/e2e_conformance.rs::e2e_conformance_trip_canonical` and the focused `trip_loaded_*` cases                                                                                                                                                                                                                                                                                                                                                             | The explicit ignored compatibility tier compares construction and loaded channels plus normalized DVI with the pinned TRIP oracle. `tests/it/e2e_conformance/assets.rs` fails when its registered local oracle is absent unless conformance oracles are explicitly optional.                                                        |
| e-TRIP                        | `crates/umber/tests/it/e2e_conformance.rs::e2e_conformance_etrip` and `tests/it/e2e_conformance/etrip_official.rs`                                                                                                                                                                                                                                                                                                                                                           | The explicit ignored full-document tier compares the e-TeX run with the pinned e-TRIP channels, DVI, and official projection.                                                                                                                                                                                                       |
| Exhaustive tracer ordering    | `tools/tex-command-stream/tests/it/command_semantic.rs::declared_command_semantic_cases_match`, `src/compare/tests.rs`, and the `tex-command-stream` CLI                                                                                                                                                                                                                                                                                                                     | The routine tests bind declared cases and channel contracts. `cargo run -q -p tex-command-stream --bin tex-command-stream -- --repository .` is the integrated ordered-event gate and must end `CLEAN`.                                                                                                                             |
| DVI artifacts                 | `crates/umber/tests/it/e2e_conformance.rs::e2e_conformance_story_canonical`, the seven `canonical_*_reference_dvi` cases in that module, `tests/it/cli.rs::run_math_corpus_matches_committed_dvi`, `run_align_corpus_matches_committed_dvi`, and `crates/tex-exec/tests/it.rs::fresh_and_memo_shipouts_share_canonical_artifact_dvi`                                                                                                                                         | Byte comparison permits only the normalization declared by each fixture. Fresh, restored, memoized, and CLI publication routes must emit the same committed DVI.                                                                                                                                                                    |
| PDF artifacts                 | `crates/umber/tests/it/pdf_parity.rs::committed_pdftex_fixtures_match_structure_and_bytes` and the atomic publication failures in `tests/it/cli.rs`                                                                                                                                                                                                                                                                                                                          | Six committed cases execute exact-byte, reference-structure, Umber-structure, raster, and render-attestation channels. Failed finalization must not replace requested output.                                                                                                                                                       |
| CLI                           | `crates/umber/tests/it/cli.rs::exits_successfully`, `lex_dump_prints_stable_token_format_for_corpus`, `expand_dump_prints_stable_token_format_for_corpus`, `run_exec_corpus_matches_committed_diagnostics`, `run_etex_exec_corpus_matches_committed_diagnostics`, and `run_usage_errors_follow_existing_shape`                                                                                                                                                               | Process status, stdout/stderr, committed terminal/log channels, usage diagnostics, and published files are the contract. Profiling allocation censuses are not CLI compatibility.                                                                                                                                                   |
| Format dump, load, and cache  | `crates/umber/src/format_fixture/tests.rs::independent_raw_builds_are_byte_identical_and_cache_reload_is_fresh`, `raw_etex_cache_reuse_reloads_exact_live_registry_into_fresh_runtime_state`, `production_pdftex_cache_reuse_reloads_exact_live_registry_into_fresh_runtime_state`, `crates/umber/src/prepared_format/tests.rs::every_loaded_job_has_fresh_clock_terminal_and_mutable_state`, and `tests/it/cli.rs::run_publishes_a_dumped_format_from_the_resource_session` | Canonical bytes and logical content survive dump/load; every loaded job gets fresh mutable runtime state; validation and publication remain atomic. Runtime generation keys and capacities are excluded from the format ABI.                                                                                                        |
| Resource suspension and retry | `crates/tex-incr/src/tests.rs::candidate_retries_staged_missing_input_without_losing_state`, `multi_round_resource_retry_drops_orphan_fragment_bytes_and_keeps_parity`, `crates/umber/src/engine_session.rs::resource_suspension_does_not_publish_or_latch_termination`, and `crates/umber/src/tex_fixed_point/tests.rs::tex_only_resource_wait_resumes_the_same_pass`                                                                                                       | A typed need publishes no provisional effects, fulfillment resumes once without replayed dispatch, and accepted effects, artifacts, and DVI equal a cold run with the same resource snapshot.                                                                                                                                       |
| Diagnostics                   | `crates/tex-exec/tests/fixture_parity.rs`, `crates/umber/tests/it/cli.rs::run_exec_corpus_matches_committed_diagnostics`, `run_etex_exec_corpus_matches_committed_diagnostics`, and `run_show_publishes_canonical_error_context`                                                                                                                                                                                                                                             | Ordered terminal/log text, recovery status, help/context, provenance presentation, and fatal versus recoverable completion remain fixture-identical.                                                                                                                                                                                |
| Checkpoints                   | `crates/tex-exec/src/checkpoint/tests.rs::retained_checkpoint_restores_command_and_mode_token_roots`, `crates/tex-incr/src/tests.rs::no_op_revision_converges_and_preserves_cold_output`, and `changed_included_input_rejects_checkpoint_reuse`                                                                                                                                                                                                                              | Restore reconstitutes future command/mode behavior, valid convergence preserves DVI, and changed dependencies force safe fallback. Whether an optional hash or physical owner is present is not asserted.                                                                                                                           |
| Rollback and atomic rejection | `crates/umber/tests/it/effectful_replay.rs`, `tests/it/replay_identity.rs`, `crates/tex-exec/src/mode/tests.rs::journal_nested_commit_and_rollback_compose`, and `crates/tex-incr/src/tests.rs::restored_suffix_resource_candidate_retries_once_and_rejects_atomically`                                                                                                                                                                                                      | Generated programs restore semantic state and every effect channel; nested commit/rollback composes; rejected candidates leave the accepted revision, source, checkpoints, effects, artifacts, and DVI unchanged.                                                                                                                   |
| Incremental revisions         | `crates/tex-incr/src/tests.rs::semantic_edit_scenario_matrix_is_cold_identical_without_false_convergence`, `edited_output_is_byte_identical_to_a_fresh_cold_session`, `fast_scripted_edit_sequence_matches_cold_every_revision`, and `src/tests/long_session.rs::long_session_revisions_and_retries_match_clean_semantics`                                                                                                                                                   | Every accepted edit is compared with a fresh cold session. Long sequences combine accepted edits, retried rejected candidates, checkpoint schedules, effects, artifacts, DVI, and future-state comparison without asserting a storage implementation. The 1,000-edit and 2,048-cycle variants remain explicit ignored stress tiers. |

## Deleted compatibility assertions

The following assertions were removed or replaced by the semantic owners above:

- `tex-exec` mode-summary tests no longer compare `Arc` pointers; they compare
  the preserved summary and independently mutated restored semantics. The
  stale journal-generation assertion was removed while nested rollback and
  non-innermost rejection remain covered.
- `tex-exec` transaction-protocol tests no longer pin `CommandPreflight` size,
  exact owner bitsets, mark projections, or owner/admission error variants.
  Exhaustive primitive classification remains.
- `EngineCheckpoint::has_exact_state_identity` and the `tex-incr` assertions
  that merely checked presence of an optional live-state identity were
  removed. Convergence now proves the selected boundary, reuse result, and
  byte-identical DVI.
- Synthetic private-revision probes, fabricated unrooted allocations, private
  domain statistics, exact ownership censuses, and layout-generation equality
  were removed from `tex-incr`. Retry and rejection tests instead compare
  accepted effects, artifacts, DVI, source layout, and checkpoint state.
- The long-session test no longer freezes live owner counts, weak-index bucket
  capacities, generation charges, journal counts, or RSS. It repeatedly proves
  cold semantic equivalence and stable work receipts across accepted and
  retried rejected revisions.
- The `VirtualCompileSession` Rust-size ceiling, VFS generation-number checks,
  and profiling `HOT_CORE_CENSUS` schema/count tests were removed from `umber`.
  Virtual compilation still proves atomic root/generated-file/output
  publication and exact rollback after failure.

These deletions intentionally leave no requirement to recreate the outgoing
`Arc`/`Weak`, region, census, generation-number, or fixed-layout substrate.
Performance and bounded-memory requirements belong to architecture-specific
post-rewrite gates, not to compatibility assertions about the old mechanism.
