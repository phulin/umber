# Alignment and brace semantics

Status: implementation contract for TeX82 alignment token delivery.

## Canonical boundary

The canonical sources are `third_party/texlive-source/src/texk/web2c/tex.web`
and the matching
`third_party/texlive-source/src/texk/web2c/pdftexdir/pdftex.web`.
pdfTeX retains TeX82's alignment scanner, macro argument, conditional,
`scan_toks`, `off_save`, and `do_endv` rules; its extensions do not introduce
a second alignment depth.

Umber follows one invariant: `tex_command::AlignmentDeliveryState` owns the
only `align_state`, beside the one raw `get_next` loop. It is the value
produced by raw delivery, not semantic group depth. The executor may request
alignment lifecycle transitions but cannot classify an already delivered token.
A physical catcode-1 token adds one and a
physical catcode-2 token subtracts one. Control-sequence aliases such as
`\bgroup` and `\egroup` can open or close execution groups but do not affect
`align_state`. A top-level tab, `\span`, or `\cr` starts the v-template exactly
when the value is zero.

### Delimiter and template lifecycle

TeX82's `get_next` recognizes an active-cell terminator at `align_state=0`
before main control receives an ordinary command. It then takes the
v-template path. This is the lifecycle specified by `get_next`, `init_col`,
v-template insertion, `fin_col`, and `do_endv` in `tex.web` §§343 and
765--772. A scalar `get_x_token` probe, such as `scan_keyword` in
`scan_rule_spec`, can be the consumer that observes this boundary. In that
case command processing completes the typed v-template handoff and restarts
its expanded loop immediately, exactly as TeX82 §25 does after §24's
interception. It must not back up or convert the intercepted delimiter to
`endv`. The ordinary alignment-delivery path reports the original TeX82
delimiter identity (`tab`, `span`, `cr`, or `crcr`) at state 0 before selecting
the same v-template transition. In either path, the intercepted delimiter is
not separately observed as an ordinary raw command. When the interception
occurs inside TeX82 §§1034--1038 `main_loop_lookahead`, that lookahead resumes
inside the v-template: a final body character and an initial v-template
character remain in one ligature/kern run. The run ends at §1131 `do_endv`,
before `fin_col` advances across the saved tab or span. At the later retained
v-template boundary, `get_next` first delivers frozen
`end_template`; TeX82 §343 expands it to frozen `endv`, and §1131's `do_endv`
then walks the input stack expecting to find that frame still live. `do_endv`
pops nothing: §357's `end_token_list` retires the depleted frame the next time
`get_next` reaches it, which is also where the retirement is observed. With a
non-empty `\everycr`, §799's `begin_token_list(every_cr,every_cr_text)` buries
the frame first, so it survives the hook and any `\noalign` group in it, and
retires only at the `align_peek` that follows §1133's `no_align_group` brace.
Retention itself is not a retirement observation. During §343 expansion, `cur_cs` becomes `frozen_endv`, so a later
`back_input` replays effective `endv`; the two inaccessible frozen slots still
retain the same observable `endtemplate` control-sequence identity (TeX82
§§343, 765).

The shared typed delivery driver represents lookahead observation ownership as
`AlignmentLookahead::Committed` or `AlignmentLookahead::PendingExpanded`.
Only the latter commits an expanded-delivery record before a consumed command
or backup; e-TeX's raw protected terminal is always `Committed`. This is an
event distinction, not a boolean caller convention.

The sentinel values have their source meanings:

- `-1000000` scans an alignment preamble;
- `1000000` disables cell termination during row lookahead, u-template replay,
  and v-template replay;
- `0` is the cell-body base depth.

For TeX82 §37's `\omit` branch, the later delimiter does not replay the
selected column's v-template: `init_col` selects the canonical
`omit_template`, which consists only of the end-template boundary. Command
state records this as an empty retained v-template level and marks its
push/retirement as `omit`; the input reason remains `alignment_template`.

`tex_exec::mode::AlignState` owns row, column, span, and packaging progress. It
deliberately has no brace-depth shadow. Nested alignments suspend and restore
the complete outer `CommandState` alignment context, matching `push_alignment`
and `pop_alignment`.

When a box begins in an active cell, its required body opener follows the
ordinary box scanner path: command delivery validates the brace, backs it up,
and replays it before the box group is entered. If that body starts a nested
alignment, replay requests `Suspend` for the outer alignment before `Begin`
for the inner one. TeX82 §37's `align_peek` delivers a closing right brace
before replay requests the typed inner `Finish`; `fin_align`/`pop_alignment`
then resumes the saved outer delivery context before its next delimiter is
read. Thus `tex-exec` may maintain a structural stack of boxes and
alignment packaging contexts, but it never consumes, classifies, or restores
the raw brace/token state saved by `push_alignment`/`pop_alignment` (TeX82
§§37, 765--772).

Instrumentation carries a brace delivery's pre-change and committed
`align_state` directly from `tex-command`. The host-only command-stream
translator maps those begin/end-group records to canonical `state_change`
events. It also projects the replay lifecycle's structural `align_ptr`
equivalent into canonical nesting: `Begin` pushes, `Finish` reports then
pops, and `Suspend`/`Resume` retain the saved outer depth. Replay identities
remain opaque handles, so their monotonic allocation never becomes a nesting
value (TeX82 §37 `push_alignment`/`pop_alignment`, `align_peek`, and
`fin_align`).

## Canonical transition map

| Canonical operation                          | State transition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Umber owner                                                                                                                           |
| -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| `init_align` / `push_alignment`              | save outer delivery state; install `-1000000`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `tex_exec` request, `CommandState::begin_alignment` / `suspend_alignment`                                                             |
| preamble opening brace                       | run TeX82 §645 `scan_spec` in full: §407 `scan_keyword` reads and backs the brace up once for `to` and once for `spread`, then §403 `scan_left_brace` reads it a third time and _consumes_ it. The three reads and two backups are the keyword scans' own; the brace is never replayed. The clause's value is the alignment's packing specification (§805). §776 then enters `aligning`, restores `-1000000`, and retires the exhausted backup through raw preamble delivery                                                                                | `CommandProcessor::{scan_alignment_preamble_opening,begin_alignment_preamble_scan}`                                                   |
| preamble restart                             | assign `-1000000`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | `tex_command::CommandState::set_alignment_preamble_phase`                                                                             |
| raw preamble collection                      | retain raw delivery through `#`, `&`, and the terminating `\cr`; TeX82 §760 uses distinct u- and v-template loops. Its u-loop accepts `#`, recognizes only an empty-u `&&` as `cur_loop`, and otherwise reports missing parameter for a top-level `&` or `\cr` before `#`; `back_error` restores that delimiter, after which the v-loop immediately consumes it as its terminator. The v-loop instead reports/discards an extra `#` and continues. Freeze u/v pairs, publish preamble completion, then restore the live `aligning` scanner status to normal | `tex_command::CommandProcessor::begin_alignment_preamble_scan`                                                                        |
| first cell after preamble completion         | after the completion and `aligning`-to-normal observations, transfer the frozen columns once and request the executor-selected first cell; command processing delivers and backs up the first non-`\\omit` lookahead (including an unbraced `\\vrule`) before the typed template-install request pushes the ordinary u-template, including an empty list, as required by TeX82 §765                                                                                                                                                                         | `CommandProcessor::scan_alignment_cell_opening`, `AlignmentRequest::InstallCellTemplate`, `tex_exec::CommandReplayControl`            |
| u-template lifecycle observation             | after the command-owned opener backup correction, publish the exact ordinary `u_template` input push and template-push event, including an empty token list; on exhaustion, publish its retirement before restoring the cell-body sentinel from `1000000` to `0`                                                                                                                                                                                                                                                                                            | `CommandState::install_alignment_cell_template`, `CommandProcessor::retire_and_restart`, `tex_exec::CommandReplayControl`             |
| physical or replayed brace in `get_next`     | `{`: `+1`; `}`: `-1`; classification writes that exact committed adjustment into the caller's final `CurrentCommand` slot before raw observation                                                                                                                                                                                                                                                                                                                                                                                                            | `tex_command::CommandProcessor::get_next`                                                                                             |
| control-sequence brace alias                 | no depth change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `tex_command::CommandProcessor::get_next`                                                                                             |
| exhausted u-template                         | if state is above 500000, assign `0`; otherwise interwoven-preamble failure                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `CommandProcessor::retire_and_restart` and its exact replay marker                                                                    |
| top-level tab, `\span`, or `\cr`             | convert to frozen end-template delivery and assign `1000000`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `tex_command::CommandProcessor::get_next`                                                                                             |
| unbalanced cell delimiter (`align_error`)    | after raw and expanded delimiter observation, TeX82 §1102 backs up the exact delimiter. At brace depth `+1` it inserts a right brace; at `-1` it inserts a left brace. Command delivery performs both backup corrections, so the inserted brace replays before the original delimiter is intercepted at state zero. `tex-exec` receives only the opaque typed recovery event.                                                                                                                                                                               | `CommandProcessor::{get_x_alignment_delivery,recover_alignment_unbalanced_delimiter}`                                                 |
| scalar `get_x_token` delimiter probe         | complete the command-owned v-template handoff and restart expansion; the intercepted delimiter is never converted to `endv`                                                                                                                                                                                                                                                                                                                                                                                                                                 | `CommandProcessor::begin_scalar_alignment_v_template`                                                                                 |
| completed cell-body scanner                  | resume through typed expanded alignment delivery, so an ordinary main-control delimiter remains an opaque end-template event                                                                                                                                                                                                                                                                                                                                                                                                                                | `tex_exec::CommandReplayControl`, `CommandProcessor::get_x_alignment_delivery`                                                        |
| `\omit` cell                                 | `init_col` consumes the already-expanded omit lookahead, assigns `0` from `1000000`, and bypasses both backup and u-template push                                                                                                                                                                                                                                                                                                                                                                                                                           | `CommandProcessor::{scan_alignment_cell_opening,scan_alignment_next_cell_opening}`, typed `AlignmentRequest::InstallOmitCellTemplate` |
| `align_peek` and `fin_col` restart           | assign `1000000` before lookahead/u-template; `align_peek` has already backed up the first nonblank command, so it installs the selected u-template immediately, while `fin_col` performs the next-entry lookahead before installation                                                                                                                                                                                                                                                                                                                      | `tex_exec::CommandReplayControl`, `CommandProcessor::scan_alignment_next_cell_opening`                                                |
| `align_peek` `\\noalign` branch              | assign `1000000`; command-owned expanded delivery recognizes `no_align`, then command-owned `scan_left_brace` advances to `1000001` without a backup; executor enters `no_align_group` and resumes paragraph mode                                                                                                                                                                                                                                                                                                                                           | `CommandProcessor::scan_alignment_noalign_opening`, typed replay no-align group transition                                            |
| `back_input` / `back_error`                  | consume the command's recorded delivery adjustment once before replay and publish the correction after the backup lifecycle transition; a stale stamp cannot apply it again                                                                                                                                                                                                                                                                                                                                                                                 | `tex_command::CommandProcessor::back_input`                                                                                           |
| macro delimiter ending in `#{`               | cancel the duplicate opening-brace adjustment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `tex_command::macro_call`                                                                                                             |
| macro extra `}` recovery                     | undo backup accounting, then let inserted recovery input account normally                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `tex_command::macro_call`, `CommandProcessor` backup replay                                                                           |
| aborted macro argument with unmatched groups | remove its accumulated imbalance                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `tex_command::macro_call` argument-level correction                                                                                   |
| alphabetic constant `` `} `` / `` `{ ``      | cancel `get_token` brace accounting                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | `tex_command::scanners::scalar`                                                                                                       |
| compulsory-brace recovery                    | inserted `{` contributes one even when synthesized outside raw delivery                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `tex-command` scanners and `CommandProcessor` inserted-token delivery                                                                 |
| ordinary `scan_toks`                         | real opening and closing braces account through raw delivery                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `tex_command::scan_toks`                                                                                                              |
| expanded `scan_toks` detached replay         | add one synthetic opening-brace level, remove only that level, retain expansion-produced deltas                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `tex_command::scan_toks` and `CommandProcessor` expansion                                                                             |
| box-body closer after alignment work         | package only on the delivered closer for the active box save-stack group; alignment depth remains token-delivery state                                                                                                                                                                                                                                                                                                                                                                                                                                      | `tex_exec::box_runtime::packaging`, `CommandProcessor`                                                                                |
| `pass_text` conditional skipping             | discarded tokens still pass through `get_next`; their braces change depth                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `tex_command::conditionals`, `CommandProcessor` raw delivery                                                                          |
| `\ifx` operands                              | unexpanded operands still use `get_next` brace accounting                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `tex_command::conditionals`                                                                                                           |
| nested alignment / `pop_alignment`           | restore the exact outer state, active identity, and cell templates                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `CommandState::{suspend_alignment,resume_alignment}`                                                                                  |
| `do_endv`                                    | accept only an exhausted active v-template (or its exact driver-carried marker); TeX82 §1131's stack walk also skips an exhausted scanner-backed effective `endv` immediately above it. The walk inspects; it pops nothing                                                                                                                                                                                                                                                                                                                                  | `tex_exec::align::execution`, `CommandState::finish_alignment_cell`                                                                   |
| successful `do_endv`                         | commit the §791 `fin_col` state change only. The depleted v-template (and any exhausted backup above it) retires through TeX82 §357's `end_token_list` at the next `get_next`, which is also where both retirements are observed                                                                                                                                                                                                                                                                                                                            | `CommandState::{prove_endv_input_shape,finish_alignment_cell}`                                                                        |
| `off_save` above bottom level                | TeX82 §§1064, 1066, and 1131 first report `off_save_replay`, then command-owned recovery backs up the effective end-v without undoing its already-committed delimiter transition and inserts the closer selected from the actual execution group; replayed end-v then finishes the retained v-template                                                                                                                                                                                                                                                      | typed executor group selection plus `CommandProcessor::recover_endv_off_save`                                                         |
| `off_save` at bottom level                   | drop the offending command; do not replay it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `tex_exec::assignments::off_save_alignment`                                                                                           |
| `align_error` at depth `-1` or `1`           | back up delimiter, insert the missing brace, and return to zero through ordinary delivery                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `tex_exec::align::execution::run_cell_body_until_terminator`                                                                          |
| alignment closing brace before `\cr`         | after typed replay selects its active entry `align_group`, TeX82 §1103 backs up the delivered brace (including its ordinary backup correction), then command processing inserts immutable frozen `\cr`; it does not close the group, so v-template delivery precedes brace replay. This follows the same command-owned recovery ordering as `off_save` (§§1064, 1066, and 1131) and `align_error` (§1102).                                                                                                                                                  | `CommandProcessor::recover_alignment_closing_brace`, `tex_exec::CommandReplayControl`                                                 |
| exhausted preamble tab/span after `do_endv`  | TeX82 §772 `fin_col` retains the raw saved delimiter through v-template completion. When typed replay finds no next column and no periodic suffix, it requests command-owned recovery; command state changes the saved tab/span to the row outcome (`cr_code`) and publishes `extra_tab` before the `do_endv` state change. The executor selects only the exhausted-preamble structural branch and never reclassifies raw delimiter input.                                                                                                                  | `AlignmentRequest::RecoverExtraTab`, `AlignmentDeliveryState::recover_extra_tab`, `tex_exec::CommandReplayControl`                    |
| `align_peek` closing right brace             | command processing delivers and accounts for the brace; typed replay then performs `Finish` and resumes the saved outer context before its next delimiter                                                                                                                                                                                                                                                                                                                                                                                                   | `tex_exec::CommandReplayControl`, `CommandState::{finish_alignment,resume_alignment}`; TeX82 §37, `fin_align`/`pop_alignment`         |
| `fin_align` / `pop_alignment`                | retire inner level and resume saved outer level                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | `tex_exec::align`, `CommandState::{finish_alignment,resume_alignment}`                                                                |
| failed nested alignment                      | discard the incomplete inner level and restore the outer level atomically                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | `CommandState::resume_alignment`, execution transaction rollback                                                                      |

## Regression ownership

Focused regressions live at the owning boundary:

- aliases, `do_endv` recovery, and command-owned outer-delivery resumption:
  `crates/tex-exec/src/main_control/tests.rs`;
- raw brace accounting, nested suspension, and v-template retirement:
  `crates/tex-command/src/input/tests.rs`,
  `crates/tex-command/src/state/tests.rs`,
  `crates/tex-command/src/snapshot/tests.rs`, and
  `crates/tex-command/src/processor/alignment/tests.rs`;
- skipped braces and `\ifx`: `crates/tex-command/src/conditionals/tests.rs`;
- macro arguments and expanded definitions:
  `crates/tex-command/src/macro_call/tests.rs` and
  `crates/tex-command/src/scan_toks/tests.rs`;
- integer-scanner corrections:
  `crates/tex-command/src/scanners/scalar/tests.rs`.
- canonical delivery/lifecycle observation:
  `tests/tex82-oracle/instrumentation.ch` and
  `tests/tex82-oracle/transitions.tex`; the detached schema records semantic
  nesting and transition classes without pointer or input-stack identities.
- replay observer ordering for the first u-template opener and retirement:
  `crates/tex-command/src/processor/alignment/tests.rs`.
- structural nesting projection across nested `fin_align`/`pop_alignment`:
  `tools/tex-command-stream/src/lib.rs`.
- TeX82 §§791--805 periodic template/tabskip identity, cell-adjustment
  migration, quarterword span bounds and merge fields, width recurrence, and
  final set-list traversal:
  `crates/tex-command/src/scanners/structured/tests.rs` and
  `crates/tex-exec/src/align/{execution,packaging,widths}/`.

The fixture-only native correctness gate remains `cargo test --tests`; format
and clippy remain `scripts/check.sh`.
