# Tooling surface disposition

Status: owner decision for `umber2-vgjr.18.1`

This inventory decides which package, command, benchmark, trace, prototype,
fuzz, and associated script surfaces may be changed by `umber2-vgjr.18.2`
through `umber2-vgjr.18.4`. It is not a deletion list. In particular, absence
from routine CI is not retirement evidence: explicit live-reference,
performance, profiling, and fuzz tiers are intentionally outside
`cargo test --tests`.

## Dispositions

- **Supported compatibility** is an operator-facing command or public Rust
  surface whose retirement has not been approved. It survives until an
  equivalent command is proved or a separate compatibility decision retires
  it.
- **Active gate** has an assertion, budget, comparison, publication, or
  failure-classification contract. A gate may be explicit and opt-in rather
  than part of routine CI.
- **Reproducible diagnostic** answers a current engineering question with a
  pinned workload or protocol but does not itself decide pass/fail for the
  repository.
- **Historical surface** records a completed choice and has no surviving
  compatibility, gate, or diagnostic authority. This document approves only
  these rows for deletion by `umber2-vgjr.18.4`; that issue must still verify
  the stated dependencies and preserve the cited receipt in documentation or
  Git history.

An _external invocation_ below means the documented command available to a
maintainer outside a Cargo test. “None documented” is a negative audit result,
not permission to delete the surface.

## Reference execution and parity

| Candidate                                                    | Repository evidence                                                                                                                                                                                | External invocation evidence                                                                                                                       | Owner decision and surviving authority                                                                                                                                                                                                                                               | Dependency mapping                                                                      |
| ------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------- |
| `refexec` library reference-process and `tftopl` kernel      | `fixturegen` depends on and calls `RefTex`; `scripts/regen-fixtures.sh` builds `refexec`; fake-executable behavior is covered by `tools/refexec/tests/refexec.rs`                                  | `UMBER_REF_TEX` and `UMBER_REF_TFTOPL` are documented in `tools/AGENTS.md` and `tests/AGENTS.md`                                                   | **Supported compatibility.** Preserve executable lookup and overrides, deterministic environment, staging, flags, stdout/log/DVI capture, failure status, and `tftopl` behavior. `fixturegen` is the destination publication owner, but the kernel moves only with behavioral proof. | `18.2`, after completed `12.4` typed fixtures and `2.3` comparison policy consolidation |
| `refexec` CLI, including existing-DVI comparison             | The binary is a root workspace/default member; `scripts/run-latex-corpus.sh` invokes its existing-DVI comparison, while `scripts/regen-fixtures.sh` invokes its reference-run and comparison paths | `refexec <file.tex> ...`, `--compare-dvi`, and `--compare-existing-dvi` are advertised by the binary and exercised through those supported scripts | **Supported compatibility.** Keep a behaviorally equivalent compatibility command or obtain a new explicit retirement decision. Generic DVI normalization/equality survives in `test-support`.                                                                                       | `18.2`; CLI removal is gated separately from moving the library kernel                  |
| `parity-harness` committed-fixture comparison and triage     | Umber end-to-end tests call the default library; `trip_triage.rs` owns compact TRIP reports; `scripts/check-tools.sh` tests both default and `reference-tools` resolutions                         | `parity-harness --compare-existing-dvi ...` and `cargo test -p parity-harness self_test_bundle_pinpoints_page_and_opcode` are documented           | **Active gate.** Retain comparison status, normalized DVI bytes, bounded triage artifacts, and TRIP event-first reports. `test-support` owns generic DVI equality; `tex-command-stream` owns semantic comparison policy; parity owns conformance composition and triage.             | `18.2`; the owners were established by completed `2.3` and `12.4`                       |
| `parity-harness` live `reference-tools` CLI path             | `scripts/run-latex-parity.sh` builds and invokes the feature-enabled binary; `scripts/regen-fixtures.sh` builds the same resolution for external-document publication                              | `scripts/check-latex-parity.sh` and `scripts/run-latex-parity.sh` are documented live-reference tiers                                              | **Active gate.** Preserve the LaTeX cohort's flags, environment, staging, status, DVI/report bytes, and failure classes until `18.2` supplies and proves the contracted replacement. Publication itself survives under `fixturegen`.                                                 | `18.2`; do not delete merely because routine Cargo tests are hermetic                   |
| Public `run_named_external_document` live composition helper | The symbol is feature-gated in `parity-harness`; repository search finds no Rust caller                                                                                                            | No dedicated operator command beyond the feature-enabled parity CLI is documented                                                                  | **Supported compatibility.** It is a public feature API and remains until `18.2` either retains a thin composition adapter or records a separate API retirement.                                                                                                                     | `18.2`; negative caller evidence is explicitly insufficient                             |
| `fixturegen --reference-dvi`                                 | `scripts/regen-fixtures.sh` uses fixturegen/parity publication for Story, Gentle, TRIP, and e-TRIP; fixturegen atomically publishes the verified bytes                                             | The sole supported entry point is `scripts/regen-fixtures.sh --area e2e` or the matching provisioning flow, not direct authority mutation          | **Active gate.** `fixturegen` remains the sole live-reference fixture publication owner. Preserve manifest hash verification, atomic publication, and rollback.                                                                                                                      | Survives `18.2`; may absorb the minimal reference kernel                                |

## Fixture layout and publication commands

| Candidate                                                                              | Repository evidence                                                                                                                                                            | External invocation evidence                                                                                                                                            | Owner decision and surviving authority                                                                                                                                                                                                                                                   | Dependency mapping                                                            |
| -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| `fixturegen --migrate-layout --plan/--apply` and its declarative registry              | `layout_migration.rs` and its tests cover the completed execution, lexical/session, and bibliography layout migrations; current fixture consumers no longer call the migration | Both modes and their digest/rollback contract remain documented in `tools/AGENTS.md` and `docs/testing_infrastructure.md`; no ordinary regeneration script invokes them | **Supported compatibility.** Preserve the command through `18.3` while extracting any transaction code still shared by active publication. `18.3` may retire the one-time registry and command only as an explicit compatibility change, retaining the current-tree plan/digest receipt. | `18.3`; completed `12.4` establishes the typed surviving fixture contract     |
| `fixturegen --migrate-pdf-layout --plan/--apply` and its fixed registry                | `pdf_layout_migration.rs` stages the completed 15-case PDF cohort through the shared transaction engine; ordinary PDF regeneration now uses `pdf.rs`                           | Both modes remain documented in `tools/AGENTS.md`; no ordinary regeneration script invokes them                                                                         | **Supported compatibility.** Same retirement rule as `--migrate-layout`; the PDF cohort and its closed-case authority survive independently of this one-time command.                                                                                                                    | `18.3`                                                                        |
| `fixturegen --cohort-transaction --plan/--apply`                                       | Classic BibTeX regeneration invokes it, and `command-semantic-channels` emits plans for it; transaction tests cover validation, rollback, retry, and garbage collection        | The versioned JSON command and result schema are documented in `tools/AGENTS.md`                                                                                        | **Active gate.** This is the shared atomic publication authority, not obsolete migration scaffolding. Preserve it or move it with all schema, ownership-digest, rollback, recovery, and committed-cleanup semantics.                                                                     | Survives `18.3`; helpers may be extracted before retiring one-time registries |
| `fixturegen --area`, `--case`, PDF regeneration, corpus sync, and classic BibTeX modes | `scripts/regen-fixtures.sh` and `scripts/provision.py` are tracked callers                                                                                                     | `scripts/regen-fixtures.sh` is the sole supported live-reference rewrite entry point                                                                                    | **Active gate.** These are ordinary publication/acquisition modes and are outside migration-command retirement.                                                                                                                                                                          | No deletion in `18.3`; `18.2` may change only the reference-kernel dependency |

## Benchmarks and prototypes

| Candidate                                                | Repository evidence                                                                                                                                                                              | External invocation evidence                                                                                                                                                              | Owner decision and surviving authority                                                                                                                                                                                                         | Dependency mapping                                                                     |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| Plain TeX workload rows and `scripts/bench-plain-tex.sh` | Seven committed workloads validate completion markers and stable normalized artifacts across Umber and available TeX engines                                                                     | `scripts/bench-plain-tex.sh`; `BENCH_INPUTS`, `BENCH_RUNS`, and `UMBER_ONLY` select a reproducible local comparison                                                                       | **Reproducible diagnostic.** Retain all seven rows and the shared preamble; they measure distinct engine phases and are not correctness evidence.                                                                                              | `18.4` may rehome, not delete or silently rebaseline                                   |
| Edit-restart workload pairs and generated `long` row     | `gentle-profile --edit-restart-workload` loads the committed pairs, verifies cold equivalence, and validates their hashes; the deletion baseline cites them                                      | README hash check plus the `gentle-profile` commands in `docs/profiling.md`                                                                                                               | **Reproducible diagnostic.** These are the surviving neutral workloads for future incremental designs even though paragraph replay was deleted.                                                                                                | Retain under `18.4`; depends on the current named-boundary restart authority           |
| `tex-command` `command_allocations` rows                 | The binary exercises named command-core operations and has a perturbation sensitivity check                                                                                                      | Documented release `cargo run` command and `--perturb`                                                                                                                                    | **Reproducible diagnostic.** Retain every named row; before/after results require the same host, toolchain, profile, and revision.                                                                                                             | `18.4` may rehome beside `tex-command`                                                 |
| `tex-command` `source_descriptor` row                    | It measures current generated-source descriptor identity reuse and uses production `tex-command`                                                                                                 | Documented Criterion invocation in `docs/profiling.md`                                                                                                                                    | **Reproducible diagnostic.** Retain while descriptor identity remains a production optimization question.                                                                                                                                      | `18.4` may rehome beside `tex-command`                                                 |
| `tex-exec` `widths` rows and `node-width-budgets.json`   | `scripts/check-node-width-budget.sh` executes the rows and enforces the committed 10% limits                                                                                                     | `scripts/check-node-width-budget.sh`                                                                                                                                                      | **Active gate.** Preserve row names, workload sizes, baseline values, and tolerance.                                                                                                                                                           | `18.4` may rehome only with byte-identical baseline authority                          |
| `tex-exec` `layout` rows and `layout_allocations`        | The Criterion rows include explicit deep stack-safety workloads; the allocation binary enforces ceilings for the same pure kernels                                                               | Commands are documented in `benchmarks/tex-exec/README.md`                                                                                                                                | **Active gate.** Preserve stack-safety workloads and allocation ceilings.                                                                                                                                                                      | `18.4` may rehome beside `tex-typeset`/`tex-exec`; do not count moved code as deletion |
| `tex-exec` `shipout` rows                                | Both current artifact-lowering paths are exercised with fixed 1,024-node inputs                                                                                                                  | Documented Criterion invocation                                                                                                                                                           | **Reproducible diagnostic.** Retain as the current shipout-lowering comparison.                                                                                                                                                                | `18.4` may rehome beside the surviving artifact owner                                  |
| `tex-exec` `pure_memo_edit` rows                         | The benchmark uses the retained bounded pretolerance/page/shipout memo runtime through a real `tex-incr::Session`                                                                                | Criterion target is present but was omitted from the README command list; the canonical invocation is `cargo bench --manifest-path benchmarks/tex-exec/Cargo.toml --bench pure_memo_edit` | **Reproducible diagnostic.** Retain and document the command; paragraph replay deletion did not delete bounded pure caches.                                                                                                                    | `18.4`; depends on `PureMemoRuntime`, not the retired paragraph replay design          |
| `tex-exec` `dvi_page_snapshot` comparison                | Added by `40782ecf6` to compare the former deep clone with the adopted shared immutable collection; it is synthetic and does not exercise a production call boundary                             | Documented Criterion invocation                                                                                                                                                           | **Historical surface.** The adopted representation and correctness tests are the authority; approve benchmark deletion in `18.4`, retaining the commit and the README description as the measurement receipt until deletion lands.             | `18.4`; verify no new consumer or baseline appeared since this decision                |
| `tex-exec` `mode_list_rollback` comparison               | Added as the design experiment and followed by production journal promotion in `74afafab9`; the authoritative implementation and acceptance evidence are in `docs/mode_list_rollback_journal.md` | Documented Criterion invocation                                                                                                                                                           | **Historical surface.** The synthetic COW-versus-watermark prototype no longer gates the implemented journal. Approve deletion in `18.4`; production journal tests, source-boundary gate, and semantic comparison remain.                      | `18.4`; preserve the durable measurement/implementation document                       |
| `tex-state` `snapshot_budgets` and `snapshot_gate`       | `snapshot_gate --enforce` owns latency-scaling and retained-allocation assertions; the Criterion rows report paired measurements                                                                 | `scripts/check-snapshot-budgets.sh` and focused `--workload=...` documented in `docs/snapshot_performance.md`                                                                             | **Active gate.** Preserve workload identities, constants, allocation semantics, and failure behavior.                                                                                                                                          | `18.4` may rehome beside `tex-state`, not delete                                       |
| `tex-state` `state_budgets`                              | Assertion-bearing provenance rows enforce the documented retained-byte limit; other rows measure current state operations                                                                        | Focused Criterion commands are documented in `docs/testing_infrastructure.md`, `docs/profiling.md`, and `crates/tex-state/AGENTS.md`                                                      | **Active gate.** Preserve assertion-bearing rows and their constants; nonasserting rows remain reproducible diagnostics in the same owner crate.                                                                                               | `18.4`; splitting is permitted only without baseline drift                             |
| `tex-state` `accepted_edit_scaling`                      | Exercises current `FragmentStore` append/snapshot scaling and can report allocations                                                                                                             | Canonical Criterion invocation is `cargo bench --manifest-path benchmarks/tex-state/Cargo.toml --bench accepted_edit_scaling`; no separate operator document was found                    | **Reproducible diagnostic.** Retain until the accepted-fragment representation changes or a later owner decision retires the question.                                                                                                         | `18.4`; negative documentation evidence is not deletion approval                       |
| `tex-state` `dependency_gate`                            | Exercises production dependency recording with disabled and enabled regions; production and tests still use the API                                                                              | Canonical release invocation is `cargo run --release --manifest-path benchmarks/tex-state/Cargo.toml --bin dependency_gate`; no separate operator document was found                      | **Reproducible diagnostic.** Retain as the overhead control for current dependency observation.                                                                                                                                                | `18.4` may document/rehome beside `tex-state`                                          |
| `tex-state` `format_cache_profile`                       | Exercises the production cache miss/store/hit/repeated-store and direct-decode paths                                                                                                             | Release invocation is documented in `docs/format_cache.md`                                                                                                                                | **Reproducible diagnostic.** Retain as the format-cache investigation tool.                                                                                                                                                                    | `18.4` may rehome beside the cache owner                                               |
| PNG importer prototype                                   | Added by `e410c02f8` to compare the former custom path and `png::Reader` with the adopted `StreamingDecoder`; it is a standalone crate and does not execute Umber                                | README pins three arXiv sample paths, pixel count, SHA-256 identities, and a release command                                                                                              | **Historical surface.** The decoder choice is implemented and the prototype owns no production or gate authority. Approve deletion in `18.4` after retaining the command, input hashes, method, and result provenance in Git/decision history. | `18.4`; deletion must not remove the current PDF/image correctness and corpus gates    |

## Traces and fuzzing

| Candidate                                                                        | Repository evidence                                                                                                                                                                | External invocation evidence                                                                                      | Owner decision and surviving authority                                                                                                                                           | Dependency mapping                                                                          |
| -------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| Gitignored full-document TeX82 trace registry                                    | `tex-command-stream` optionally loads the three manifest-bound traces; the builder verifies clean/instrumented transparency, repeatability, sources, fonts, and atomic publication | `scripts/build-tex82-document-traces.sh --document NAME`, followed by the documented `tex-command-stream` command | **Reproducible diagnostic.** Retain builder, manifest, document templates, and explicit partial/missing status. These large traces are not routine correctness fixtures.         | `18.4` may rehome with `tex-command-stream`; no deletion as “orphan”                        |
| Canonical TeX82/e-TeX/pdfTeX oracle traces and command-semantic channel captures | Regeneration scripts validate schema, repeatability, clean/instrumented transparency, and committed channel authority; Cargo tests consume committed projections and channels      | Only the pinned `scripts/regen-fixtures.sh --oracle ... --profile ...` commands are supported                     | **Active gate.** These traces and captures are canonical fixture evidence, not cleanup candidates.                                                                               | Outside `18.4` historical deletion; ownership remains `tex-oracle`/`tex-observe`/fixturegen |
| Public generic `tex_incr::{TraceOperation, TraceSummary}`                        | The public re-exports have active unit tests but repository search finds no production caller                                                                                      | No operator command or external caller is recorded in the repository                                              | **Supported compatibility.** Keep the public API; its lack of an internal caller is not retirement approval. Any later deletion needs the repository's public Rust API decision. | `18.4` must not delete it                                                                   |
| Incremental scripted fuzz tier                                                   | The ignored exact test compares one thousand revisions with cold execution; the wrapper names the exact test                                                                       | `scripts/test-incremental-fuzz.sh`                                                                                | **Active gate.** Retain as the deterministic edit-mapping/convergence fuzz tier.                                                                                                 | `18.4` may rehome the wrapper only if the named gate remains runnable                       |
| Effectful rollback/commit proptest tier                                          | The Umber integration test checks pre-commit leakage, rollback state identity, and committed-prefix replay; the wrapper controls the case budget                                   | `scripts/effectful-rollback-fuzz.sh` and `PROPTEST_CASES=...`                                                     | **Active gate.** Retain outside the default suite because its intended 10,000 cases are deliberately expensive.                                                                  | `18.4` may rehome, not weaken or delete                                                     |
| Cargo-fuzz `tfm_parse` target and seed                                           | The standalone `fuzz` crate targets the production TFM parser and carries the `cmr10.tfm` seed                                                                                     | Standard explicit invocation is `cargo fuzz run tfm_parse`; no repository wrapper is present                      | **Reproducible diagnostic.** Preserve the Cargo-fuzz boundary and seed. Lack of CI invocation is explicitly not retirement evidence.                                             | `18.4` may add a documented wrapper; no deletion                                            |

## Script disposition summary

The candidate-specific rows govern their scripts. For clarity:

- `scripts/regen-fixtures.sh`, `scripts/check-tools.sh`,
  `scripts/check-latex-parity.sh`, `scripts/run-latex-parity.sh`,
  `scripts/check-node-width-budget.sh`, and
  `scripts/check-snapshot-budgets.sh` are **active gates** or the sole supported
  entry point to one.
- `scripts/bench-plain-tex.sh` and
  `scripts/build-tex82-document-traces.sh` are **reproducible diagnostics**.
- `scripts/test-incremental-fuzz.sh` and
  `scripts/effectful-rollback-fuzz.sh` are explicit **active gates**.
- No script is historical merely because CI does not call it. The only
  historical decisions in this inventory are the DVI snapshot benchmark, the
  mode-list rollback prototype benchmark, and the PNG importer prototype.

## Handoff

- `umber2-vgjr.18.2` may move the reference-process kernel into fixturegen and
  contract parity around comparison/triage, but must preserve or separately
  retire the two compatibility surfaces identified above.
- `umber2-vgjr.18.3` may retire the two completed layout migration commands and
  registries only as an explicit compatibility change. The shared cohort
  transaction and ordinary regeneration modes survive.
- `umber2-vgjr.18.4` may rehome active gates and diagnostics without changing
  baselines. It has owner approval to delete only the three historical rows,
  after checking that no intervening caller or authority was added.
