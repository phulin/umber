# umber2-sdpz.245: share scalable VF-leaf font resources across sizes

The PDF-success denominator remains the authenticated 87-row TeX Live 2026
capture in `docs/arxiv_census/recent-20260902-pdftex-pdf/`, ordered by its
locked `lock_order` values. No reference was regenerated by this issue.

## Durable corpus progress

Row `2605.22212`, lock order 42, is complete. Its source-derived jobname is
`paper-JDE-o4`; its clean pdfTeX and Umber normalized projections are exact at
SHA-256 `038c47236c87790cb5c18b008911fbc98a14eed943e1cd3209a9e2e920dcdbf5`.
The matched Umber PDF is
`ba25613ecb09fdd86d01ae190c0c81041eb6a026a88389a8359fc719acb3b783`.
Its exact AUX and OUT identities remain respectively
`046f06e5c9423f147955408da40f62287acdb46ff329e8e1bd74ecd987b37ae9`
and
`a7c9a980b0351fbf9cf761e238b022a40e013b2b21bd1258f369f870303210aa`;
the established exact DVI remains unchanged.

The next PDF-success row is `2606.00086`, lock order 43, entrypoint and
source-derived jobname `root_first_submission`. The archive SHA-256 is
`c83f73eba7dafef6ceb39139f97c382de321bb5c799099c14a4c0ddf7b833330`.
Its existing clean pdfTeX PDF is 6,824,424 bytes with SHA-256
`e430c3db54077bde9882b386f10c507c8ee6979707ba3479c3ff4ed50632a981`;
its normalized projection is
`d4e14324cdc4a42fcc8a79b88e1eccb7842f1ec09c068f59a15492e152132a70`.
The first Umber run completed under the standing guards but was not normalized
exact: PDF
`b6c1b466608c2037828f15404da21fd32d6955d0a603eef1210d538905871c87`,
normalized projection
`f2bcb455a54cc0e24c6e62d39debd8c4855494a3628e65b715eb4870f4c4aa81`.

## First divergence and fix

The earliest difference was page 1's font-resource dictionary. The Times
virtual fonts reach the same mapped real leaf at several sizes. Umber retained
separate realized metrics correctly but allocated a font dictionary and subset
for each size. pdftex.web section 32e's `pdf_init_font` instead searches prior
scalable fonts and shares the first font object when the TFM name and resolved
font-map entry match; size remains per TeX font through `pdf_use_font`.

Detached VF-leaf materialization now applies that rule. A size-specific
`RealizedFontIdentity` still selects the correct metrics and text-state size,
while equal mapped leaf names reuse the first resource and object identity.
The focused two-size VF control proves that both realized instances remain and
share one mapped PDF resource; absent-map PK fallbacks remain separate.

The one final authenticated row used `SOURCE_DATE_EPOCH=1772323200`,
`FORCE_SOURCE_DATE=1`, offline mode, 500,000,000 expansion fuel, 10,000,000
execution steps, a 120-second wall guard, two-second termination grace, and the
authorized 2,048-MiB aggregate-RSS ceiling. The fixed PDF SHA-256 is
`c1f3f4a11077c81e9a83f026090796309ad3bf006ce8cf9cd71f5a7113f78b0b`;
its normalized projection is
`6d9a9feeba82a8bee7770b9cfade199c5c8e4865eb7c1ccd2ba740c8660cb995`.
The Times leaves now collapse to three shared subsets across sizes. The full
AUX remains byte-exact at
`013c530c5182267623c8a92f1e5b914751f4d64c3022ed3f3f6a1df693a0a0dd`;
neither engine emitted an OUT file.

The row remains non-exact. The next first page-1 resource/content difference
is recorded without a causal theory in `umber2-sdpz.246`.

## Verification

The focused mapped VF-leaf sharing test passes. The owning Umber suite passes
526 tests; its sole failure is the inherited base failure in the font-free
`annotations_running` expected Umber bytes, which still include the no-op
`q`/`Q` pair removed before this issue. The PDF/VF change cannot affect that
fixture. The final `scripts/check.sh` result is recorded in the issue close
reason.
