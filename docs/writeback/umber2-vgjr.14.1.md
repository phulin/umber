# umber2-vgjr.14.1 — generated Biber compatibility cases

Authority is the pinned Biber 2.22 beta suite at commit
`74252e608e5f8115375c532eb25416430a9f52eb` and its byte-identified fixture
manifest. One typed compatibility contract now records upstream identity,
module, order, test name, xfail reason, effective control edits, output
requests, and explicit field, name-assignment, or BBL-slice expectations. Its
closed macro vocabulary expands every row to a separately selectable Cargo
test. The immutable runner compares cache-enabled and cache-disabled complete
results before applying the committed expectation; expected data is never
generated from the observed result.

The uniqueness, label-alpha, label-alpha-name, extra-title,
extra-title-year, and extra-date cohorts contribute 417 generated cases. The
typed completeness table owns all 51 modules and 1,275 assertions; the former
lexical assertion census is gone, while the independent source-policy audit
continues to reject bare ignores, expected-panic markers, placeholders, and
non-native comparisons. Exact names, order, xfail strings, option/control
bytes, output paths/formats, expected Unicode and BBL bytes are preserved.

The six converted shards fell from 9,649 to 4,821 lines. The 435-line shared
contract and runner make measured net authored test deletion 4,393 lines.
Focused `bib-engine` execution passed under `MemoryMax=512M` (356 passed, 939
xfails ignored). The uncapped workspace `--no-run` build and full suite under
`MemoryMax=1G` passed. `scripts/check.sh` passed all four gates under the 1 GiB
cgroup. No linked discovery was required.
