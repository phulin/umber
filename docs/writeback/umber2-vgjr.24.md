# umber2-vgjr.24 — bibliography forecast reconciliation

The audit found no remaining duplicate authority. `BiberDraft` and
`DraftEntry` are the sole mutable Biber root through one document freeze; tool
mode consumes already transformed entries and remains a distinct public mode.
Classic retains one compiler-owned scanner, compiled-style/callable model,
indexed `READ` database and cache, explicit-frame VM, and ordered log stream.
Its artifact construction is classic-backend policy and must not be routed
through the Biber output pipeline. The XML parse variants share one flat,
include-aware projection. All BBL, BibTeX, DOT, and XML compatibility
serializers delegate to `OutputRouter`, `OutputPlan`, and `OutputSink`; public
result and format-failure names are aliases rather than parallel engines.

The typed compatibility manifest still owns exactly 51 modules and 1,275
assertions. Its 417 generated cases each retain an upstream name, order,
control edit, unique expectation, xfail disposition, cache-off comparison, and
separate test identity. The other 858 assertions, source-policy audit, classic
fixtures, byte/diagnostic/Unicode/output checks, and generated-file tests own
independent proof. Deleting or relocating them would weaken the contract or
move lines between accounting categories, not remove duplicate test authority.

Summing the six implementation commits named by Program 14 independently
reproduces 1,334 additions and 1,743 deletions in production Rust (net -409),
3,017 additions and 7,429 deletions in authored Rust proof tests (net -4,412),
and 140 additions and 44 deletions in documentation/maps. Total tracked change
is 4,491 additions and 9,216 deletions (net -4,725), with no fixture, generated,
binary, or configuration change. Reaching the forecast floors would require a
further 491 production and 1,588 test net deletions for which the audit found no
contract-preserving inventory.

After that conclusion was reported, the portfolio owner explicitly accepted
the measured production -409 and test -4,412 results, retired the 900--1,700
production and 6,000--9,000 test forecasts, and directed that no shortfall or
unearned moved/generated/declarative/total-line credit carry forward. The
51-module/1,275-assertion identity, 417 generated cases, named production
authorities, retained legacy APIs, and all proof gates remain unchanged.

On source tree `bc6359e606c1c504cc83a5d2e06fdf7faafe11e4`, the focused bibliography
and complete workspace scopes compiled uncapped with six Cargo jobs. Under a
512 MiB cgroup and finite timeouts, the manifest gate selected exactly 1,275
upstream assertions, the compatibility run passed 337 and retained 938 explicit
xfails, and the combined focused run passed 72 engine unit, 356 integration, 23
input, 12 output, and four Unicode tests with its declared ignores. This covers
the generated cases and classic, Biber, XML, Unicode, diagnostic, output, and
legacy API boundaries. The unchanged complete routine suite passed under a 1
GiB cgroup and finite timeout. Uncapped `scripts/check.sh` with six Cargo jobs
reported that all four gates passed. No execution used `prlimit` or serialized
the test harness.
