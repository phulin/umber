# `umber2-alfh`: Test-tier and oracle-contract closeout

The epic closes with all 36 direct children and all 149 parent-child
descendants closed. The historical 99-of-203 manual-failure snapshot is not a
current census: the corpus now has 207 cases, and its exact ignored gate passes.

The final legacy execution census is 81 cases. The active owners and tier
rationale are recorded in `docs/golden_corpus_dispositions.md`: `align` owns
10, `etex_exec` 1, `exec` 1, `expand` 8, `math` 19, `tex_exec` 29, and
`typeset` 13. Retired `dvi`, `page`, and `tex_exec_io` evidence has exact
command-semantic or pdfTeX-extension replacements.

The TeX82 catalogue reports 1,029 reviewed and 351 deferred modules, with 132
covered properties and 24 honest gaps. Every gap names a non-`.32` owner, and
the exact active-test-link gate proves that the 15 compatibility properties
retired with the old executor have canonical replacements. The separate
pdfTeX catalogue authenticates its pinned 1.40.29 WEB evidence and verifies
complete, unique, active status/terminal/log ownership for every retained
extension case.

Every comparable command-semantic terminal, log, DVI, and effects channel is
reference-backed; no current manifest uses `unsupported`. The closeout
promoted three stale openout-log xfails after all three exact fingerprints
XPASSed. The only remaining strict dispositions have live owners:

- `umber2-johp.762` owns deferred top-level write artifact bytes;
- `umber2-johp.763` owns immediate no-shipout artifact materialization; and
- reopened `umber2-mp0d` owns the interaction-mode terminal-check projection,
  whose collector currently includes log-only effects.

Ten pass projections were refreshed from obsolete stringly summaries to the
current typed observation vocabulary. These are Umber-side drift pins, not
reference-channel authority changes. The final 207-case manual gate, exhaustive
tracer, both property catalogues, and both Story DVI gates pass on the closeout
tree.
