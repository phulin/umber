# umber2-vgjr.15 — active test-authority closeout

Audited implementation tree: `7d540b78d9a5c8bede5c166826b667ddb7e4d725`.

## Authority result

The dormant execution ledger contains exactly 447 original cases. Of these,
445 are active in the `tex-exec` library target; the removed admissibility and
whole-attempt-retry cases have explicit current owners. All 64 former
`cfg(any())` sites, the disabled library-test setting, and every source-audit
exception are absent. The routine audit discovers tracked production Rust and
Cargo target state itself, and its independent negative fixtures prove both
rejection paths and stale-exception handling.

The TeX82 ledger contains 94 affected rows. The 35 formerly dormant-citing
properties have 54 current `tex-exec` coverage links, all resolving to active
`#[test]` functions; the remaining 29 uncovered ledger rows name explicit gap
Beads. The complete catalogue gate reports 946 reviewed modules, 434 deferred
modules, 112 covered properties, and 39 gap properties. The alignment and job
documentation citations resolve to active exact-input tests; the discovery
review is explicitly historical rather than test authority.

The command ledger deletes no semantic case. Every cited scanner value, token
and event order, exact recovery string, lifecycle, rollback, framing, and
identity assertion remains active. In particular, profile identity cites
`format_and_checkpoint_profile_components_reject_mismatch`, which checks every
exact/Unicode TeX82, e-TeX, and pdfTeX profile against both format and
checkpoint boundaries.

The typesetting/browser ledger retains rule matrices, deep-stack and storage
bounds, exact HTML and semantic projections, worker transfer and teardown,
hostile-input atomicity, DOM identity/lifetime, and real-browser fixtures. The
one retired wire test is assertion-complete across active binding tests. The
largest accepted JavaScript integer `9_007_199_254_740_991` and first rejected
integer `9_007_199_254_740_992` both cross the actual `JsValue` DTO. No digest,
self-generated fixture, declaration-only comparison, or similarly named case
substitutes for those assertions.

## Exact cumulative accounting

| Category                    | Additions | Deletions |      Net |
| --------------------------- | --------: | --------: | -------: |
| Authored Rust               |       879 |     1,042 |     -163 |
| Documentation and guidance  |     1,614 |       619 |     +995 |
| Declarative catalogue/Cargo |        67 |        51 |      +16 |
| **Tracked program total**   | **2,560** | **1,712** | **+848** |

The semantic compaction children account for `+568/-1,042` authored Rust, net
`-474`; the executable recurrence audit accounts for `+311/-0`. Documentation,
fixtures, generated sources, binary assets, and moved or historical code are
not credited as deletion.

## Independent verification

With `CARGO_BUILD_JOBS=6`, fresh native and wasm32 builds completed uncapped.
Under `MemoryMax=512M`, the source audit, six catalogue tests, `tex-exec`,
`tex-command`, `tex-typeset`, and `tex-out` passed. Under `MemoryMax=1G`, the
complete native suite passed; all 91 Node tests, the three WASM wire tests, two
typed virtual-font tests, packaged Node lifecycle, and npm dry-run passed. The
optimized WASM package built uncapped. Final uncapped `scripts/check.sh` passed
all four gates. Every runtime had a finite timeout and no `prlimit` was used.

Firefox is absent, so the Firefox browser tier is `BLOCKED`. The Chrome runner
reached its explicit preflight and stopped at `/usr/bin/google-chrome`
`ENOENT`. Neither unavailable real-browser tier is reported as passing.
