# umber2-vgjr.14 — bibliography simplification closeout

The program preserved the pinned Biber 2.22 beta identity at commit
`74252e608e5f8115375c532eb25416430a9f52eb` and every committed fixture byte.
One typed compatibility manifest owns 51 modules and 1,275 assertions. Its 417
generated cases remain separately selectable, retain upstream order, names,
xfails, control edits, output requests, Unicode, and expected bytes, and compare
cache-enabled with cache-disabled complete results before checking committed
expectations.

The surviving production authorities are one engine-private `BiberDraft` and
one immutable document freeze; one classic explicit-frame VM with
compiler-owned scanning, indexed `READ`, typed cache inputs, unified callables,
and ordered log events; one flat include-aware XML projection arena; and one
closed output plan and router-owned bounded sink. The eager Biber datasource,
entry editor, graph/result protocols, classic lexer transfer, string-keyed
projection, parallel cache/report models, recursive XML tree, configuration
intermediate in the session, output selection graph, open serializer protocol,
and independent format failure/finalization authorities are gone. Public legacy
result aliases, serializer entry points, failure aliases, and Unicode values
remain compatible thin surfaces rather than second engines.

Exact implementation-commit accounting is:

| Category                         | Additions | Deletions |    Net |
| -------------------------------- | --------: | --------: | -----: |
| Production Rust                  |     1,334 |     1,743 |   -409 |
| Authored Rust proof tests        |     3,017 |     7,429 | -4,412 |
| Documentation/repository maps    |       140 |        44 |    +96 |
| Fixtures/generated/configuration |         0 |         0 |      0 |
| Total                            |     4,491 |     9,216 | -4,725 |

The authored Rust net is -4,821 lines. The measured production and test
deletions are below the plan's forecast floors, so `umber2-vgjr.24` owns either
further principled deletion or an explicit forecast revision; no future or
historical deletion is credited here.

On exact implementation tree `8c7c496916454ed6df47e628d1692e817a20e510`,
uncapped focused and full `cargo test -q --tests ... --no-run` builds passed.
Under hard cgroup caps and finite timeouts, the focused bibliography selection
passed at 512 MiB (72 engine unit, 356 active compatibility, 23 input, 12
output, and four Unicode tests; 940 declared ignores), and the unchanged full
routine suite passed at 1 GiB. Node WASM ran two tests, and the packaged
TeX--bibliography--TeX lifecycle passed at 1 GiB; release package compilation
peaked at 668,672,000 bytes. `scripts/check.sh` passed all four gates at 1 GiB
with `CARGO_BUILD_JOBS=1`.

One earlier full-suite attempt failed because the produced story DVI lacked a
valid preamble. The provisioned oracle matched its lock and primary-checkout
SHA-256; the exact test and a second unchanged full suite passed. The event is
recorded as `umber2-dgsn`, not reported as a green first attempt or attributed
to bibliography behavior.
