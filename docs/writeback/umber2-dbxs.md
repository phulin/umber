# umber2-dbxs: coherent frozen LaTeX release line

The owned mismatch was resolved by selecting the LaTeX `2026-06-01`
pre-release line already present in the pinned TeX Live 2026-03-01 tree. The
source lock now pairs kernel `latex.ltx` identity
`d3362038dc2c0e1af921065473b5baac03b49d2763ad85d7337fe2384d5d04ae`
with first-aid identity
`da3142f2495d4b2106eb74263f0ce86771e3a84d26048b0d30c23aafe185eeed`.
The publisher stages the complete authenticated construction closure first, so
the frozen format and each runtime basename winner share one release line.

The schema-11 rebuilds were each repeated twice and passed source-loaded
equivalence under the standard 500,000,000-fuel, 600-second, and 2,048-MiB
format guards. LaTeX is
`9ea2783f3000423606a274974145f7b07580753bbd53682a10c3a55b3f4b9fd9`
(1,987,908 bytes); pdfLaTeX is
`1dce7159b8c974ebfd896a74edff4c3f6870497ccb28fc3979b2cdf5ef773a6f`
(2,030,265 bytes). Both name source manifest
`ba49b8698d222b16afc26811c03d52125b632545c963ad8b506a6939e91925db`.
The coherence smoke asserts the kernel-owned first-aid controls, including
`\newcounteralias`, and executes the pinned amsthm after-hook by declaring a
shared-counter theorem.

The authenticated sparse publication reused base root
`43a31da364e4607957a38da10dabff227657d607d1845d502204adfd5d002e4b`.
Two clean successor outputs matched byte for byte and produced root
`61b8d665e492662b18c8beb70ab8cd8a8f73d9bd7e4d9aeb2f958ea8613f8883`,
with 322 staged objects totaling 102,653,889 bytes. External manifest-last R2
deployment could not run in this environment because it has neither R2
credentials nor a configured remote, so default hosted identities were not
changed.

The exact 18 archived identities were rerun offline with unchanged
100,000,000 fuel, 120-second, 1,536-MiB, and 2-second TERM-grace guards. Run
identity is
`e2b4c14b60aa61f2aca578926beb2f5b3b02a527fe3ced983f7cbf9c82a4887f`;
the verified outcome is
`2a9d6e8c573cfda9228401e516f4947414319e9aa65970cde287c481ceceb4d8`.
The old `undefined-control-sequence` four-frame signature is absent from every
row. Ten PDFs completed. Later outcomes were split into unclaimed successors:
`umber2-tps1` owns five node-list timeline panics, `umber2-rat7` owns the new
nine-frame terminal-execution EOF, `umber2-8tvk` owns the accepted
`xybtip10.pfb` finalizer failure, and `umber2-fhrx` owns the accepted finalizer
guard timeout.
